#include <stdio.h>

/**
 * main - nums fron  0 to 9 .
 *
 * describtion: nums print.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 48; i < 58; i++)
	{
		putchar(i);
	}
	for (int i = 97; i < 103; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
