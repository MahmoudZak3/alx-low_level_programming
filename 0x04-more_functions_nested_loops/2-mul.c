#include "main.h"
/**
 * mul - multiply tow int degets
 * @a: first deget
 * @b: second deget
 * Return: 1 if succes or 0 else if not succes addition of the multiplicattion
 */

int mul(int a, int b)
{
	return (a * b);
}
